// Canvas3D component - disabled to avoid 3D elements in background
// Use StarFieldBackground.tsx for star-based background instead

const Canvas3D = () => {
  return null;
};

export default Canvas3D;