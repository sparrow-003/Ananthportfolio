"use client"

// This component is no longer needed as we're using CSS animations instead
// Keeping the file to avoid breaking imports, but it's empty

export function ParticlesBackground() {
  return null
}
